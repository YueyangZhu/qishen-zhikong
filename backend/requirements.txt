# 契审智控 后端依赖
# Python 3.10+ 推荐

# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20  # 文件上传支持

# 配置与校验
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# PDF 解析（pdfplumber 比 pypdf 更适合合同场景，能保留文本位置和表格结构）
pdfplumber==0.11.5

# DOCX 解析
python-docx==1.1.2

# 大模型调用（OpenAI 兼容 SDK，DeepSeek 使用 OpenAI 协议）
openai==1.59.6

# HTTP 客户端（备用，用于其他 API 调用）
httpx==0.28.1

# PDF 报告生成（reportlab 支持中文字体与表格布局）
reportlab==4.2.5

# Playwright（PDF 报告生成 - 无头浏览器渲染）
playwright==1.61.0

# Supabase 客户端（数据库 + Auth）
supabase==2.9.0

