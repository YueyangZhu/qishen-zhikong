fix(P08): 重大风险统计修正 + 法务操作可撤回 + 人工风险可删除

1. fix(logic.ts): getMajorRisks 包含 pending 状态，人工添加的
   重大风险（status=pending）现在也会被统计到右侧统计区

2. add(riskService): 新增 del 方法，删除风险（仅限人工添加的风险，
   sourceType='manual'），支持审计日志记录

3. add(db/dataApi): 新增 apiDeleteRisk / deleteRisk 底层方法

4. feat(LegalRiskCard): 法务操作增强：
   - 已确认/已编辑/转人工复核状态的风险：显示「撤回操作」按钮，
     可一键恢复到待处理状态
   - 人工添加的风险（sourceType === 'manual'）：显示「删除风险」
     按钮，二次确认后删除
   - 均采用乐观更新 + 失败回滚